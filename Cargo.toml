[workspace]
members = ["."]

[package]
name = "compositor"
version = "0.1.0"
edition = "2024"

[dependencies]
smithay = { git = "https://github.com/Smithay/smithay", branch = "master", default-features = false, features = [
    "backend_winit",
    "wayland_frontend",
    "desktop",
] }
